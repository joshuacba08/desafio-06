doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet")
    body
        main.min-h-screen.w-screen.py-6.bg-yellow-50.flex.items-center.justify-center.flex-col
            h1.text-4xl.uppercase.py-4.mb-4.font-bold Conocé nuestros productos

            .grid(class='grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4')
                if arrayProducts
                    each product in arrayProducts
                        div.bg-white.w-72.h-96.shadow-md.rounded.m-3
                            div(class='h-3/4 w-full')
                                img.w-full.h-full.object-cover.rounded-t(src=`${product.thumbnail}` alt=`${product.title}`)
                            div(class='w-full h-1/4 p-3')
                                a(href="#" class='hover:text-yellow-600 text-gray-700')
                                span.text-lg.font-semibold.uppercase.tracking-wide #{product.title}

                else
                    p no hay products
                p hola  